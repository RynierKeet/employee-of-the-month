# Database connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=yourpassword
DB_NAME=employee_of_the_month

# Session secret for express-session
SESSION_SECRET=your-session-secret

# Server port
PORT=3000

# Temporary login password when employee.password_hash is NULL
# Change this in production and share out-of-band with admins only.
TEMP_LOGIN_PASSWORD=Temp1234!